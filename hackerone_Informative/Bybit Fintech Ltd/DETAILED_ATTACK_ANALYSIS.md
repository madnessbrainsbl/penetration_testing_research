# 🔬 DETAILED ATTACK LOG & DEFENSE ANALYSIS: BYBIT

**Target:** api.bybit.com / www.bybit.com
**Date:** Nov 24, 2025
**Status:** All attacks mitigated by defense mechanisms.

Ниже приведен подробный технический разбор каждой проведенной атаки, полученных ответов и архитектурных причин неудачи.

---

## 1. Authentication Bypass & Signature Spoofing

Мы пытались обойти требование подписи `X-BAPI-SIGN`, чтобы отправлять неавторизованные запросы или подделывать параметры.

### ⚔️ Вектор атаки
1.  **Empty Signature:** Отправка запроса с пустым заголовком.
2.  **Signature Replay:** Повторная отправка перехваченного валидного запроса.
3.  **Parameter Tampering:** Изменение `amount` в теле запроса без изменения подписи.
4.  **Timestamp Manipulation:** Отправка времени из будущего/прошлого.

### 📉 Ответ сервера
*   **Empty/Wrong Sig:** `{"retCode": 10004, "retMsg": "error sign!"}`
*   **Replay/Timestamp:** `{"retCode": 10002, "retMsg": "invalid request, please check your server timestamp or recv_window param"}`
*   **Empty Key:** `{"retCode": 10001, "retMsg": "empty value: apiKey"}`

### 🛡️ Почему это не сработало (Анализ защиты)
Bybit использует строгую реализацию **HMAC-SHA256**.
1.  **Целостность:** Сервер берет *все* параметры + timestamp + api_key, хеширует их своим секретом и сверяет с вашим `X-BAPI-SIGN`. Любое изменение 1 байта в параметрах ломает хеш.
2.  **Защита от Replay:** Параметр `X-BAPI-TIMESTAMP` и `recv_window` (обычно 5000мс) проверяются на сервере. Если запрос старее 5 секунд — он отклоняется, даже если подпись валидна. Это делает replay-атаки невозможными без генерации новой подписи (для которой нужен Secret Key).

---

## 2. IDOR (Insecure Direct Object Reference)

Мы пытались получить доступ к данным других пользователей (кошелек, ордера, API ключи), подменяя идентификаторы.

### ⚔️ Вектор атаки
1.  **GET Parameter:** `/v5/account/wallet-balance?uid=1` (вместо моего UID).
2.  **POST Body:** `{"memberId": "1", ...}`.
3.  **Parameter Pollution:** `?uid=MY_UID&uid=TARGET_UID`.
4.  **Array Injection:** `?uid[]=1`.
5.  **JSON Type Confusion:** `{"uid": 1}` (integer) vs `{"uid": "1"}` (string).

### 📉 Ответ сервера
*   **Status:** `200 OK`
*   **Body:** Возвращаются данные **моего** аккаунта.
*   **Log:** `{"retCode": 0, "result": { "list": [ { "memberId": "MY_ACTUAL_ID", ... } ] } }`

### 🛡️ Почему это не сработало
Архитектура API Bybit **игнорирует клиентский input** для идентификации пользователя.
*   Контекст пользователя (`user_ctx`) извлекается **строго из API Key**, переданного в заголовке.
*   Параметры `uid` / `memberId` в теле запроса либо не используются вовсе, либо сверяются с токеном, и при несовпадении приоритет отдается токену. Это "Secure by Design" подход.

---

## 3. CORS Misconfiguration (Cross-Origin Resource Sharing)

Мы нашли, что сервер разрешает запросы с произвольных доменов.

### ⚔️ Вектор атаки
Отправка запроса с заголовком `Origin: https://attacker.com`.

### 📉 Ответ сервера
*   **Header:** `Access-Control-Allow-Origin: https://attacker.com`
*   **Header:** `Access-Control-Allow-Credentials: true`

### 🛡️ Почему это не сработало (False Positive)
Хотя конфигурация выглядит опасно (разрешает всё + credentials), эксплуатация невозможна из-за **механизма аутентификации**.
*   CORS опасен, когда браузер автоматически отправляет **Cookies** или **Basic Auth**.
*   Bybit использует кастомные заголовки `X-BAPI-API-KEY` и `X-BAPI-SIGN`.
*   Браузер **никогда** не добавит эти заголовки автоматически. JS атакующего должен добавить их вручную, но он не знает `API Secret` жертвы, чтобы подписать запрос.
*   **Итог:** Дверь открыта (CORS), но ключа от сейфа (Signature) у атакующего нет.

---

## 4. Hidden API / Deadpool SDK (`/v3/config`)

Мы пытались найти скрытые конфигурации через endpoint, найденный в JS.

### ⚔️ Вектор атаки
1.  **Fuzzing:** Перебор `project_name`: "admin", "internal", "dashboard", "deadpool".
2.  **Header Spoofing:** Подмена `platform: pc`, `X-User-Agent`.
3.  **Method Tampering:** Попытка использовать GET вместо POST.

### 📉 Ответ сервера
*   **Большинство:** `404 Not Found` (ресурс не существует).
*   **Payload `debug: true`:** `403 Forbidden`.

### 🛡️ Почему это не сработало
*   **404:** Скорее всего, старые конфигурации были удалены, а код в JS остался как "мертвый груз" (legacy code).
*   **403:** Сработал **WAF (Tencent EdgeOne)**. Он детектирует подозрительные ключевые слова (вроде `debug`, `admin`) в теле JSON и блокирует запрос на уровне периметра, даже не допуская его до бэкенда.

---

## 5. Websocket Race Conditions

Мы пытались подписаться на приватные каналы через WS до прохождения авторизации.

### ⚔️ Вектор атаки
1.  Открытие WS соединения.
2.  Мгновенная отправка `{"op": "subscribe", "args": ["wallet"]}` (без `op: auth`).
3.  Отправка malformed JSON.

### 📉 Ответ сервера
*   `{"success": false, "ret_msg": "Please auth first", "op": "subscribe"}`
*   `{"success": false, "ret_msg": "conn_id invalid"}`

### 🛡️ Почему это не сработало
Состояние соединения проверяется **атомарно**.
*   Пока не пройдет handshake авторизации (`op: auth`), соединение имеет флаг `UNAUTHENTICATED`.
*   Любой запрос к приватным топикам проверяет этот флаг. Race condition невозможен, так как проверка флага происходит в том же потоке обработки сообщения, что и подписка.

---

## 6. Injection Attacks (SQLi, XSS, SSRF)

Попытки внедрить вредоносный код.

### ⚔️ Вектор атаки
1.  **SQLi:** `' OR '1'='1` в параметрах фильтрации.
2.  **SSRF:** Webhook registration на `http://127.0.0.1`.
3.  **XSS:** Внедрение скриптов в User-Agent или Referer.

### 📉 Ответ сервера
*   Запросы либо блокировались (TCP Reset / Timeout) — работа WAF.
*   Либо возвращали `200 OK` с пустым результатом.

### 🛡️ Почему это не сработало
1.  **WAF (Tencent EdgeOne):** Очень агрессивно фильтрует спецсимволы (`'`, `"`, `;`, `--`).
2.  **Backend:** Использование ORM или Parameterized Queries (Prepared Statements) делает SQL-инъекции невозможными, даже если WAF обойден.
3.  **SSRF:** Сервер валидирует URL вебхуков (whitelist доменов или запрет приватных IP диапазонов).

---

## 🏁 Вывод

Bybit защищен по принципу **"Defense in Depth"** (Эшелонированная защита):

1.  **Периметр:** WAF отсекает шум и сканеры.
2.  **Протокол:** Требование подписи запросов убивает подделки и replay.
3.  **Приложение:** Auth-check based on Token (не ID) убивает IDOR.
4.  **Инфраструктура:** Разделение прав и отсутствие прямого доступа к S3/Git.

**Единственный теоретический шанс взлома** — это логические ошибки в сложных бизнес-процессах (OAuth, Referral, 2FA restore), которые автоматика не может понять, а WAF не может заблокировать, так как трафик выглядит легитимно.
